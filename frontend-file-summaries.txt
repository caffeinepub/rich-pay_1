{
  "frontend/src/components/EmployeeEditModal.tsx": "Modal for editing an employee's full name and username; fixes the type error by looking up the full EmployeeRecord (including password) via getEmployeeById before calling updateEmployee.",
  "frontend/src/pages/employee/AddBankAccountPage.tsx": "Employee form page for submitting a new bank account; uses `session.userId` as the `employeeId` and saves directly to localStorage via `useAddBankAccount`, then redirects to My Bank Accounts on success.",
  "frontend/src/pages/employee/CommissionInfoPage.tsx": "Employee page displaying the 30% commission rate prominently with a hero card, step-by-step explanation of how commissions work, and important notes about eligibility.",
  "frontend/src/pages/employee/ChatWithAdminPage.tsx": "Employee page that renders the reusable ChatThread component for a private conversation between the logged-in employee and the admin, with 3-second polling.",
  "frontend/src/pages/employee/MyProfilePage.tsx": "Employee page showing profile summary with edit form (full name, username) and a separate password change form, both persisted to localStorage.",
  "frontend/src/index.css": "Global styles defining the Rich Pay dark red OKLCH color system via CSS variables for both richpay-* custom tokens and shadcn semantic tokens, plus custom scrollbar styling.",
  "frontend/tailwind.config.js": "Tailwind config extending the theme with both shadcn semantic color tokens and the full richpay-* custom color palette (darkest, dark, darker, sidebar, card, input, primary, accent, muted, border) mapped to OKLCH CSS variables.",
  "frontend/src/lib/auth.ts": "Custom localStorage-based auth layer with session management, employee CRUD, and a new `LocalBankAccount` interface plus full bank account CRUD helpers (`getAllBankAccountsFromStorage`, `addBankAccountToStorage`, `updateBankAccountStatusInStorage`, `getMyBankAccountsFromStorage`) that persist data under a dedicated `richpay_bank_accounts` key.",
  "frontend/src/hooks/useQueries.ts": "React Query hooks for all backend and localStorage operations; bank account hooks (`useGetAllBankAccounts`, `useGetMyBankAccounts`, `useAddBankAccount`, `useUpdateBankAccountStatus`) now use localStorage helpers instead of the backend actor, fixing the employee permission error.",
  "frontend/src/pages/employee/MyBankAccountsPage.tsx": "Employee page displaying their own bank accounts fetched from localStorage via `useGetMyBankAccounts`, using the `LocalBankAccount` type with ISO date strings and unique `id` keys.",
  "frontend/src/pages/admin/AllBankAccountsPage.tsx": "Admin page listing all bank accounts from localStorage with approve/reject actions; uses `account.id` as the unique key for status updates and removed the now-unused `BankAccountEditModal` and `AccountStatus` enum imports.",
  "frontend/src/pages/admin/MyAccountsPage.tsx": "Admin page for managing personal bank accounts; reads from localStorage via `useGetAllBankAccounts` filtered by `employeeId === 'admin'`, using `LocalBankAccount` type with ISO date display."
}